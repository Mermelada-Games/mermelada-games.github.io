---
import GameButton from './GameButton.astro';

export interface Props {
    imgSrc: string;
    title: string;
    buttonText?: string;
    buttonLink?: string;
    imgAlt?: string;
    textSize?: string;
    showButton?: boolean;
    imgHeight?: string;
}

const { 
    imgSrc, 
    title, 
    buttonText = "VIEW THE GAME", 
    buttonLink = "", 
    imgAlt = "Game Image",
    textSize = "4rem",
    showButton = true,
    imgHeight = "50vh"
} = Astro.props;
---

<div style="position: relative; width: 100vw; max-width: 100vw; overflow: hidden;">
  <img src={imgSrc} alt={imgAlt} style={`width: 100vw; height: ${imgHeight}; max-height: ${imgHeight}; object-fit: cover; display: block;`} />
  <div style="position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%); color: var(--palette-9); text-align: center; width: 50vw; transition: all 0.5s ease-in-out;">
    <h2 style={`font-size: ${textSize}; margin-bottom: ${showButton ? '1.5rem' : '0'}; text-shadow: 0 2px 8px rgba(0,0,0,0.7); transition: margin-bottom 0.5s ease-in-out;`}>{title}</h2>
    {showButton && <GameButton link={buttonLink} text={buttonText} />}
  </div>
</div>
